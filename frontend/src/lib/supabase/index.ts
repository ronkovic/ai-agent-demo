/**
 * Supabase client utilities.
 *
 * Usage:
 * - In client components: import { createClient } from "@/lib/supabase/client"
 * - In server components: import { createClient } from "@/lib/supabase/server"
 * - In middleware: import { updateSession } from "@/lib/supabase/middleware"
 */

export type { Database, Json } from "./types";
